CREATE OR REPLACE FUNCTION POT(N IN NUMBER,E IN NUMBER) RETURN NUMBER
AS
POTENCIA NUMBER:=1;
M NUMBER;
BEGIN
IF E<0 THEN
	M:=-E;
ELSE
	M:=E;
END IF;

FOR I IN 1..M LOOP
	POTENCIA:=POTENCIA*N;
END LOOP;

IF E>=0 THEN
	RETURN POTENCIA;
ELSE
	RETURN 1/POTENCIA;
END IF;

END;
/
